# ==========================================
# RECRUITER COPILOT - ENVIRONMENT VARIABLES
# ==========================================
# IMPORTANT: ALL variables marked as REQUIRED must be set
# The application will NOT start without these variables

# API Configuration - ALL REQUIRED
# -----------------
# Backend API base URL - NO DEFAULT VALUE
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Request timeout in milliseconds - NO DEFAULT VALUE
VITE_API_TIMEOUT=30000

# Number of retry attempts for failed requests - NO DEFAULT VALUE
VITE_API_RETRY_ATTEMPTS=3

# Delay between retries in milliseconds - NO DEFAULT VALUE
VITE_API_RETRY_DELAY=1000

# Feature Flags - ALL REQUIRED
# -------------
# Enable CSV export functionality - NO DEFAULT VALUE
VITE_ENABLE_CSV_EXPORT=true

# Enable bulk actions on records - NO DEFAULT VALUE
VITE_ENABLE_BULK_ACTIONS=true

# Development/Testing Flags - OPTIONAL
# -------------------------
# WARNING: DO NOT USE IN PRODUCTION
# Set to true to skip actual authentication API calls (dev only)
# If not set, authentication will be required
# VITE_SKIP_AUTH=false

# User ID to use when auth is skipped
# Only used if VITE_SKIP_AUTH is true
# VITE_MOCK_USER_ID=test-user-123

# ==========================================
# PRODUCTION DEPLOYMENT CHECKLIST:
# ==========================================
# 1. Set proper VITE_API_BASE_URL for your backend
# 2. Ensure all REQUIRED variables are set
# 3. DO NOT set VITE_SKIP_AUTH or VITE_MOCK_USER_ID
# 4. Review all values match your production needs
# 5. Use docker-compose.prod.yml for production
# ==========================================